<Window x:Class="DDFight.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:DDFight.Controlers"
        mc:Ignorable="d"
        Title="Dungeons &amp; Dragons Fighting Tool" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Margin="10, 0">
            Dungeon and Dragons Fight Simulator
        </TextBlock>
        <TabControl Grid.Row="1">
            <TabItem Header="Characters">
                <Border Background="GhostWhite" BorderBrush="Gainsboro" BorderThickness="1" Grid.Row="1" Margin="10">
                    <ctrl:CharacterListUserControl x:Name="CharacterListControl" Grid.Row="1"/>
                </Border>
            </TabItem>
            <TabItem Header="Monsters">
                <Border Background="GhostWhite" BorderBrush="Gainsboro" BorderThickness="1" Grid.Row="1" Margin="10">
                    <ctrl:MonsterListUserControl x:Name="MonsterListControl" Grid.Row="1"/>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
